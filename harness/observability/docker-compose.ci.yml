# CI override — skip Grafana (agents query VictoriaLogs/Metrics directly).
# Usage: docker compose -f docker-compose.yml -f docker-compose.ci.yml up -d
services:
  grafana:
    deploy:
      replicas: 0
